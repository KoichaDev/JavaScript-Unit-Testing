// Logging to the console that interqacts with the system console (which is an external system)
// and therefore, this is technically also a side-effect

export default function log(message) {
	console.log(message);
}
